# Copy this file to `.env` and fill values. Do NOT commit your real `.env` (it's already in .gitignore).

DB_USER=postgres
DB_PASSWORD="Srirama@2"
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Booknotes

# Optional: set NODE_ENV and PORT
NODE_ENV=development
PORT=3000
